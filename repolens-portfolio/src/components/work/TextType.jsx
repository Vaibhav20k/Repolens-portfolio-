import React from 'react'

// TextType is mocked in ProjectInfo.jsx during Phase 1.
// Serves as a placeholder for character typing reveal transitions in Phase 2.
export default function TextType({ text }) {
  return <span>{text}</span>
}
