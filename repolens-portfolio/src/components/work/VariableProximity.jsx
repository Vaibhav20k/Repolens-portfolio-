import React from 'react'

// VariableProximity is mocked in ProjectInfo.jsx during Phase 1.
// Serves as a placeholder for scroll-triggered character weight proximity tracking in Phase 2.
export default function VariableProximity({ text }) {
  return <span>{text}</span>
}
